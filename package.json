{
  "name": "content-strategy-agent-website",
  "version": "1.0.0",
  "type": "module",
  "description": "Full-stack Content Strategy Agent Dashboard powered by Odysseus and Hindsight Cloud",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "backend": "npm run start --prefix backend",
    "backend:dev": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run backend:dev\" \"npm run frontend\""
  },
  "dependencies": {
    "axios": "^1.17.0",
    "concurrently": "^8.2.2"
  }
}
